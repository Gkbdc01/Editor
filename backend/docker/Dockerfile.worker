# Judge Worker Dockerfile
FROM node:18-alpine

WORKDIR /app

# Install Docker CLI
RUN apk add --no-cache docker-cli

COPY package.json package-lock.json ./
RUN npm install

COPY . .

CMD ["npm", "run", "worker"]
